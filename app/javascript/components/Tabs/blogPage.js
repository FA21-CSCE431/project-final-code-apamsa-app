import React from "react";
import TabBar from "./tabBar";

const BlogPage = () => {
  return (
    <div>
      <header>
        <TabBar tabValue={1} />
      </header>
    </div>
  );
};

export default BlogPage;
